### Install Keyring for storing credentials

`
pip install keyring
`
### Adding credentials to keyring
Run this command to save credentials 
```
python -m keyring set netaccess username
python -m keyring set netaccess password
```
### Running the script
`python netaccess.py`


